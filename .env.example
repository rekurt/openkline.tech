# Sentry DSN — public, safe to expose in a static build.
# Set as a GitHub Actions secret (Settings → Secrets → Actions) for prod.
# Umami + Yandex.Metrika are loaded directly in index.html, so only Sentry
# needs env. Leave empty locally: Sentry is a no-op in dev anyway.
VITE_SENTRY_DSN=
