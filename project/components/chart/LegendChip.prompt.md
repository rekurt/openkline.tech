Indicator legend entry overlaid top-left on the chart — color swatch, mono label, muted value, hover-revealed remove.

```jsx
<LegendChip color="var(--ind-1)" label="SMA 20" value="67,391.2" onRemove={remove} />
<LegendChip color="var(--ind-2)" label="EMA 50" value="66,847.9" />
```

Stack chips in a column at `LEGEND_MARGIN` (10px left, 16px top) over the canvas.
