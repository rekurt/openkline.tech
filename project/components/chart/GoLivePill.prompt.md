The engine's floating "Go to live →" pill — shown over the chart when the user has panned away from the live edge.

```jsx
<div style={{ position: 'absolute', right: 72, bottom: 48 }}>
  <GoLivePill onClick={goLive} />
</div>
```

Accent-blue pill, 12px medium, full-round. Position it bottom-right inside the chart area, clear of the price axis.
