The openkline stroke icon set (Lucide-style outline, 24px grid, 1.75px stroke, currentColor) — the only approved icon source. Emoji are banned brand-wide.

```jsx
<IconButton title="Trend line (T)"><Icon name="trendline" size={18} /></IconButton>
<Button active><Icon name="play" size={14} /> Live</Button>
```

Names: pointer, crosshair, trendline, hline, rect, fib, channel, trash, camera, moon, sun, play, check, x, arrowRight, plus, minus, zoomIn. Inherits text color — never hardcode a fill.
