Compact checkbox as used in the terminal's indicator dropdown — 14px box, accent-blue fill when checked.

```jsx
<Checkbox label="SMA(20)" checked={on} onChange={setOn} />
<Checkbox label="Bollinger Bands" />
```
